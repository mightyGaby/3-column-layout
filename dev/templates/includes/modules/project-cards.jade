include ../mixins.jade
include ../variables.jade

.col-xs-12.col-sm-9.col-md-10.full-height.project-list.hidden
  ul.row.full-height
    - each project in projects
      +project-card(index, project.name, project.categoryClass)
      +project-card-mobile(index, project.name, project.categoryClass)
      - index = index + 1
